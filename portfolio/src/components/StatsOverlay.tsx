import React from 'react';
import { motion } from 'framer-motion';
import '../style/StatsOverlay.css';

type StatsOverlayData = {
  xp: number
};

function StatsOverlay(props: StatsOverlayData): React.ReactElement {

  return (
    <></>
  );
}

export default StatsOverlay;